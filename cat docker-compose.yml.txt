services:
  tower:
    image: geerlingguy/docker-ubuntu2204-ansible
    container_name: tower
    privileged: true
    command: /lib/systemd/systemd
    environment:
      - container=docker
    volumes:
      - /sys/fs/cgroup:/sys/fs/cgroup:rw
      - /var/run/docker.sock:/var/run/docker.sock
    tmpfs:
      - /run
      - /run/lock
      - /tmp
    networks:
      - ansible-net

  worker1:
    image: geerlingguy/docker-ubuntu2204-ansible
    container_name: worker1
    privileged: true
    command: /lib/systemd/systemd
    environment:
      - container=docker
    ports:
      - "9443:9443"
      - "8080:80"
    volumes:
      - /sys/fs/cgroup:/sys/fs/cgroup:rw
    tmpfs:
      - /run
      - /run/lock
      - /tmp
    networks:
      - ansible-net

  worker2:
    image: geerlingguy/docker-ubuntu2204-ansible
    container_name: worker2
    privileged: true
    command: /lib/systemd/systemd
    environment:
      - container=docker
    ports:
      - "9444:9443"
      - "8081:80"
    volumes:
      - /sys/fs/cgroup:/sys/fs/cgroup:rw
    tmpfs:
      - /run
      - /run/lock
      - /tmp
    networks:
      - ansible-net

  worker3:
    image: geerlingguy/docker-ubuntu2204-ansible
    container_name: worker3
    privileged: true
    command: /lib/systemd/systemd
    environment:
      - container=docker
    ports:
      - "9446:9443"  # เขยิบพอร์ตเพิ่มขึ้น 1 เลขจาก worker2 (ไม่ชนแน่นอน)
      - "8082:80"    # เขยิบพอร์ตเพิ่มขึ้น 1 เลขจาก worker2 (ไม่ชนแน่นอน)
    volumes:
      - /sys/fs/cgroup:/sys/fs/cgroup:rw
    tmpfs:
      - /run
      - /run/lock
      - /tmp
    networks:
      - ansible-net 

networks:
  ansible-net:
    driver: bridge